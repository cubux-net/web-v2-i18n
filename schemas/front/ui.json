{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "$defs": {
    "string": {
      "type": "string",
      "minLength": 1
    }
  },
  "type": "object",
  "properties": {
    "button": {
      "type": "object",
      "properties": {
        "Add": {
          "$ref": "#/$defs/string"
        },
        "Back": {
          "$ref": "#/$defs/string"
        },
        "Cancel": {
          "$ref": "#/$defs/string"
        },
        "ChooseIcon": {
          "$ref": "#/$defs/string"
        },
        "ChooseImage": {
          "$ref": "#/$defs/string"
        },
        "Confirm": {
          "$ref": "#/$defs/string"
        },
        "Create": {
          "$ref": "#/$defs/string"
        },
        "Delete": {
          "$ref": "#/$defs/string"
        },
        "Edit": {
          "$ref": "#/$defs/string"
        },
        "ImportCsv": {
          "$ref": "#/$defs/string"
        },
        "Next": {
          "$ref": "#/$defs/string"
        },
        "No": {
          "$ref": "#/$defs/string"
        },
        "OK": {
          "$ref": "#/$defs/string"
        },
        "Save": {
          "$ref": "#/$defs/string"
        },
        "SelectAccount": {
          "$ref": "#/$defs/string"
        },
        "SelectPartner": {
          "$ref": "#/$defs/string"
        },
        "Yes": {
          "$ref": "#/$defs/string"
        }
      },
      "required": [
        "Add",
        "Back",
        "Cancel",
        "ChooseIcon",
        "ChooseImage",
        "Confirm",
        "Create",
        "Delete",
        "Edit",
        "ImportCsv",
        "Next",
        "No",
        "OK",
        "Save",
        "SelectAccount",
        "SelectPartner",
        "Yes"
      ]
    },
    "check": {
      "type": "object",
      "properties": {
        "CheckAll": {
          "$ref": "#/$defs/string"
        }
      },
      "required": ["CheckAll"]
    },
    "date": {
      "type": "object",
      "properties": {
        "label": {
          "type": "object",
          "properties": {
            "SelectRange": {
              "$ref": "#/$defs/string"
            },
            "Today": {
              "$ref": "#/$defs/string"
            },
            "since": {
              "$ref": "#/$defs/string"
            },
            "until": {
              "$ref": "#/$defs/string"
            }
          },
          "required": ["SelectRange", "Today", "since", "until"]
        }
      },
      "required": ["label"]
    },
    "field": {
      "type": "object",
      "properties": {
        "MonthLimit": {
          "$ref": "#/$defs/string"
        },
        "MonthPlan": {
          "$ref": "#/$defs/string"
        }
      },
      "required": ["MonthLimit", "MonthPlan"]
    },
    "label": {
      "type": "object",
      "properties": {
        "AskAQuestion": {
          "$ref": "#/$defs/string"
        },
        "DownloadAt": {
          "$ref": "#/$defs/string"
        },
        "FoundATypo": {
          "$ref": "#/$defs/string"
        },
        "date": {
          "type": "object",
          "properties": {
            "sinceStart": {
              "$ref": "#/$defs/string"
            },
            "untilEnd": {
              "$ref": "#/$defs/string"
            }
          },
          "required": ["sinceStart", "untilEnd"]
        }
      },
      "required": ["AskAQuestion", "DownloadAt", "FoundATypo", "date"]
    },
    "message": {
      "type": "object",
      "properties": {
        "CautionAboutDataLoss": {
          "$ref": "#/$defs/string"
        },
        "CheckFormAndFixErrors": {
          "$ref": "#/$defs/string"
        },
        "CouldNotPerformOperation": {
          "$ref": "#/$defs/string"
        },
        "NoRecords": {
          "$ref": "#/$defs/string"
        },
        "PleaseWait": {
          "$ref": "#/$defs/string"
        },
        "ThisActionCannotBeUndone": {
          "$ref": "#/$defs/string"
        },
        "UncaughtErrorExplanation": {
          "$ref": "#/$defs/string"
        },
        "UnknownError": {
          "$ref": "#/$defs/string"
        }
      },
      "required": [
        "CautionAboutDataLoss",
        "CheckFormAndFixErrors",
        "CouldNotPerformOperation",
        "NoRecords",
        "PleaseWait",
        "ThisActionCannotBeUndone",
        "UncaughtErrorExplanation",
        "UnknownError"
      ]
    },
    "placeholder": {
      "type": "object",
      "properties": {
        "Search": {
          "$ref": "#/$defs/string"
        },
        "SelectCountry": {
          "$ref": "#/$defs/string"
        },
        "SelectCurrency": {
          "$ref": "#/$defs/string"
        },
        "SelectDate": {
          "$ref": "#/$defs/string"
        },
        "SelectLanguage": {
          "$ref": "#/$defs/string"
        }
      },
      "required": [
        "Search",
        "SelectCountry",
        "SelectCurrency",
        "SelectDate",
        "SelectLanguage"
      ]
    },
    "select": {
      "type": "object",
      "properties": {
        "UseDefault": {
          "$ref": "#/$defs/string"
        },
        "payInterval": {
          "type": "object",
          "properties": {
            "Empty": {
              "$ref": "#/$defs/string"
            },
            "m": {
              "$ref": "#/$defs/string"
            },
            "q": {
              "$ref": "#/$defs/string"
            },
            "w": {
              "$ref": "#/$defs/string"
            }
          },
          "required": ["Empty", "m", "q", "w"]
        }
      },
      "required": ["UseDefault", "payInterval"]
    },
    "state": {
      "type": "object",
      "properties": {
        "Error": {
          "$ref": "#/$defs/string"
        },
        "Loading": {
          "$ref": "#/$defs/string"
        }
      },
      "required": ["Error", "Loading"]
    }
  },
  "required": [
    "button",
    "check",
    "date",
    "field",
    "label",
    "message",
    "placeholder",
    "select",
    "state"
  ]
}
